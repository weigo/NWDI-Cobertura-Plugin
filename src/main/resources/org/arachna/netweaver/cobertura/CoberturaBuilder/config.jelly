<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define"
	xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
    <f:entry title="${%encoding.title}" description="${%encoding.description}">
      <f:select name="encoding" value="${it.encoding}" field="encoding" clazz="required" />
    </f:entry>
	<f:entry title="${%title}" description="${%description}">
		<f:textbox name="junitTimeOut" value="${instance.junitTimeOut}" />
	</f:entry>
</j:jelly>
